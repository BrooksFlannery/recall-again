import { describe, it } from "bun:test";

describe("createScheduledQuizFromDueFacts", () => {
  it.skip("creates scheduled quiz + items", async () => {
    // PENDING: Patch 3
  });

  it.skip("idempotent same day", async () => {
    // PENDING: Patch 3
  });
});
