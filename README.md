# recall-again

Bun-only project: use **Bun** for installs and all scripts (`bun install`, `bun run dev`, `bun test`, etc.). See [docs/testing.md](docs/testing.md) for testing and Bun test runner conventions.
